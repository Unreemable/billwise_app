name: hhhh
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: 4.1.1
  cloud_firestore: 6.0.2
  firebase_auth: 6.1.0
  google_mlkit_text_recognition: ^0.15.0
  image_picker: ^1.2.0
  intl: ^0.20.2
  camera: ^0.11.2
  path_provider: ^2.1.5
  path: ^1.9.1
  firebase_storage: 13.0.2
  crypto: ^3.0.3

  # Local notifications & time zone
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.2


  # (اختياري) FCM إن احتجته لاحقًا
  firebase_messaging: 16.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
