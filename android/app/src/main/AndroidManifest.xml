<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Permissions ===== -->
    <!-- إشعارات (Android 13+) -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- شبكة/كاميرا -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- منبّهات دقيقة (مطلوب لبعض الجداول) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- إيقاظ الجهاز عند المنبّه -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- إعادة جدولة بعد إعادة التشغيل (لو عندك Reciever يتعامل معها) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- وسائط (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- تخزين خارجي للأجهزة < Android 13 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- الكاميرا اختيارية -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- ===== Application ===== -->
    <application
        android:name="${applicationName}"
        android:label="BillWise"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true">

        <!-- قناة الإشعارات الافتراضية لـ FCM (يجب أن تطابق القناة التي أنشأناها في Flutter: billwise_reminders) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="billwise_reminders" />

        <!-- (اختياري) أيقونة افتراضية لإشعارات FCM؛ يفضّل أيقونة أحادية اللون -->
        <!--
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        -->

        <!-- لا تُضيفي خدمة FCM يدويًا؛ تُضاف تلقائيًا من المكتبة -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- استعلامات (مثلاً للمشاركة/المعالجة) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
