<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- للاتصال بالإنترنت (Firebase، رفع الصور، إلخ) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- الكاميرا -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- قراءة الصور:
         - Android 13+ (API 33+): READ_MEDIA_IMAGES
         - Android 12 وما قبله: READ_EXTERNAL_STORAGE مع maxSdkVersion=32 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- لا تستبعدي الأجهزة بدون كاميرا (مفيد للمحاكي) -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:label="Billwise"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme يظهر فور تشغيل العملية قبل رسم Flutter -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- مطلوب من Flutter لتسجيل الإضافات -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- (اختياري) استعلام عن أنشطة معالجة النص—يستخدمه Flutter داخليًا أحيانًا -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
